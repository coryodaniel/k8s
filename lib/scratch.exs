# defmodule K8s.Resource do
#   defstruct(:api_version, :kind, :metadata, :spec, :status)
# end

# defmodule K8s.Metadata do
#   defstruct(:name, :namespace, :labels, :annotations)
# end

# defmodule K8s.ResourceList do
#   defstruct(:items, :metadata)
# end
